# Moved

Archived at [`context/archive/CARRYOVER.md`](./archive/CARRYOVER.md).

This was the task-G2 audit mapping every legacy tile-pipeline module
to a reuse/adapt/shelve fate for the overview pipeline. The modules it
audits were largely deleted in #189 (the #177 decision); the current
module map lives in `context/ARCHITECTURE.md`.

(This pointer is kept because module docs in
`crates/core/src/overview/export.rs` reference `context/CARRYOVER.md`.)
