# Moved

The sweep results and the default-value decisions they produced now
live in [`corpus/SWEEPS.md`](./SWEEPS.md). The original point-in-time
notes are archived at
[`context/archive/corpus/SWEEP_NOTES.md`](../context/archive/corpus/SWEEP_NOTES.md).

(This pointer is kept because in-code documentation — e.g. the
`--line-thinning` and `--drop-rate` CLI help — references
`corpus/SWEEP_NOTES.md`.)
