[package]
name = "tylertoo-python"
description = "Python bindings for tylertoo"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[lib]
name = "tylertoo"
crate-type = ["cdylib"]

[dependencies]
tylertoo-core = { workspace = true, features = ["remote"] }
# abi3-py39: build against CPython's stable ABI so a single wheel per
# platform covers every CPython >= 3.9 (matching `requires-python`).
# This binding is a thin facade over tylertoo-core -- it only uses the
# pyo3 safe API, no version-specific C API -- so the stable ABI costs
# nothing here.
pyo3 = { workspace = true, features = ["abi3-py39"] }
tempfile = "3"

[lints]
workspace = true
