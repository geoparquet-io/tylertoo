# mypy.stubtest allowlist.
#
# maturin wraps the compiled extension in a generated package:
#   gpq_tiles/__init__.py  ->  from .gpq_tiles import *
# The inner gpq_tiles.gpq_tiles submodule is an implementation detail of that
# wrapping; the public surface is fully described by gpq_tiles.pyi.
gpq_tiles.gpq_tiles
