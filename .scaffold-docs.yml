last_synced_sha: 81658a1842ae267a0f5ffb8c24af0b61531706d9
last_synced_date: 2026-07-20
audience:
  primary: >-
    Geospatial data engineers building tile pipelines — people with large
    GeoParquet datasets (city to planet scale) who need web-map tiles (PMTiles)
    or a queryable multi-resolution overview. Many arrive from tippecanoe and
    want Arrow-native GeoParquet integration. They work at the CLI and Python
    levels and care about throughput, memory bounds, remote reads, and output
    quality.
  secondary:
    - >-
      GeoParquet format adopters — interested in geo:overviews as a new
      GeoParquet capability (COG-style levels inside a valid, SQL-queryable
      file); care about the format, its metadata model, and interoperability.
    - >-
      Rust integrators — embedding tylertoo-core into their own services
      (convert_to_overviews, export_pmtiles, ConvertOptions, ExportOptions,
      the streaming writer); served mainly by Reference.
  inferred: false
  ask_audience: true
